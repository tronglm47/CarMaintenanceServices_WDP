// Re-export from the main firebase config
// This file is kept for backward compatibility
export * from './firebase.config';

// Export default for compatibility
export { default } from './firebase.config';