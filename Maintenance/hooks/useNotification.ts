/**
 * Hook to access notification context throughout the app
 * Usage:
 *   const { expoPushToken, notification, requestPushToken, deletePushToken } = useNotification();
 */
export { useNotification } from '@/contexts/NotificationContext';
